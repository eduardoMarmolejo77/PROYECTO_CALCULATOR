{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    // Tus dependencias de producción (React, Vue, etc.)
  },
  "devDependencies": {
    "vite": "^5.0.0" 
  }
}